#include <stdlib.h>
#include <stdio.h>
#include "slist.h"

int main(int argc, char *argv[])
{
    Slist *my_list = new_slist();
    /* slist_cons(my_list, new_cons(0)); */
    /* slist_cons(my_list, new_cons(1)); */
    /* print_slist(my_list); */
    /* free(slist_pop(my_list)); */
    /* print_slist(my_list); */
    /* free(slist_pop(my_list)); */
    /* slist_pop(my_list); */
    /* free(my_list); */
    return 0;
}

