#include <stdlib.h>
#include <stdio.h>
#include "list.h"

int main(int argc, char *argv[])
{
    List *my_list = new_list();
    /* list_cons(my_list, new_cons(0)); */
    /* list_cons(my_list, new_cons(1)); */
    /* print_list(my_list); */
    /* free(list_pop(my_list)); */
    /* print_list(my_list); */
    /* free(list_pop(my_list)); */
    /* list_pop(my_list); */
    /* free(my_list); */
    return 0;
}

